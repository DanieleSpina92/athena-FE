import React from "react";
import { Layout, Typography } from "antd";

const { Content } = Layout;

export default function HomePageContainer() {
  
  return <Typography style={{ minHeight: "100vh" }}>Athena Working progress</Typography>
}
